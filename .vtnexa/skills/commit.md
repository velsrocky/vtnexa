# commit

Write a conventional commit message for staged changes.

1. Run `git diff --staged --stat` and `git diff --staged`.
2. Reply with ONLY the message: `<type>: <summary>` + bullet body. Types: feat, fix, refactor, docs, chore.
