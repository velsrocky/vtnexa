# review

Review staged changes for bugs, then style.

1. Run `git diff --staged`.
2. Look for bugs first (logic errors, missing checks, wrong assumptions), style second.
3. Reply with findings grouped as bug / suggestion, each with file and line.
4. If nothing is wrong, say so in one line - don't invent issues.
