# test

Write or run tests for the last change.

1. Identify what changed (git diff or recent edits).
2. Write or run the relevant tests (`cargo test`, `pnpm test`, `pytest`, etc.).
3. Report pass/fail with the command you ran.
