# map

Map how a feature flows across files (entry -> logic -> output).

1. Use `skill` tools or `fs_search`/`fs_glob` to trace the feature the user names.
2. List entry points, key files/functions, and data flow in order.
3. Note any gaps or unclear connections.
4. Keep it to a short numbered flow with file:line refs.
